name: i9_team_mobile
description: i9 Team Mobile — Monitor de agentes Claude Code
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.0
  riverpod_annotation: ^2.3.0
  go_router: ^12.0.0
  dio: ^5.3.0
  flutter_secure_storage: ^9.0.0
  google_fonts: ^6.1.0
  shared_preferences: ^2.2.0
  http: ^1.2.0
  web_socket_channel: ^3.0.0
  image_picker: ^1.1.0
  pasteboard: ^0.2.0
  flutter_image_compress: ^2.3.0
  flutter_markdown: ^0.7.0
  flutter_animate: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.0
  riverpod_generator: ^2.3.0

flutter:
  uses-material-design: true
